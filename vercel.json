{
  "version": 2,
  "builds": [
    {
      "src": "netcom-crm/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".output/public"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/netcom-crm/$1" }
  ]
}
